# 宇树应用模板

欢迎使用宇树应用模板仓库！本仓库旨在为开发者提供一个**开箱即用**的环境，帮助您快速上手宇树机器人的应用开发。无论您是想使用 Python、C++、Rust 还是 ROS2 来编写应用，我们都为您准备了相应的示例代码和配置文件。

## 仓库结构
本仓库采用 Monorepo 结构，以下是本仓库的主要内容。

### 示例项目
| 目录路径                        | 示例名称              | 语言 |主要功能描述                                         |
| --------------- | --------------------- |-------- | ---------------------------------------------------- |
| `g1_walk_demo/`  | G1 行走控制 | `C++`主控，`python`提供服务 | 展示如何通过**命令行程序**控制 **G1**行走。 |
| `go2_action_demo/`| Go2 动作控制 | `python`主控，`python`提供服务 | 展示如何通过**python_sdk2**控制 **go2**运动。 |
| `go2_camera_demo/`| Go2 摄像头显示 | `python`主控，`python`提供服务 | 展示如何通过**python_sdk2**获取 **go2** 摄像头画面。 |

关于如何控制机器人的详细内容，您可以参考[宇树仓库](https://github.com/unitreerobotics) 中对应语言的 sdk。

### 辅助工具
| 目录路径                        | 工具作用               |主要功能描述                                         |
| --------------- | --------------------- |---------------------------------------------------- |
| `mobile_viewer/`  | UI 界面调试 | 提供一个简单的 `http` 服务器，方便调试移动端 UI 效果。 |
| `unitree_cli/`  | 上传版本 | 提供一个简单的 `CLI` 工具，方便在命令行中打包上传应用版本。 |

### 产物约束

`App` 使用 `docker` 作为基础设施，支持任意语言和框架，支持任意 `Linux` 发行版(`Debian、Alpine、Ubuntu22.04、Ubuntu20.04` 等)，因此您可以使用**您熟悉的任何技术开发应用**，打包发布只需要满足以下**最小要求** ：

| 文件/文件夹     | 是否必选 | 说明                                                         |
| --------------- | ---- | ------------------------------------------------------------ |
| `app`| 必选 | 应用程序文件夹，包含程序运行所需的二进制或脚本，入口在 `metadata.yaml` 中配置，需通过 **80 端口**提供您自己的 `http` 服务 |
| `ui`| 可选  | 用户前端界面文件夹，请求的**基础路径为 `/app_api/[app_name]`**，系统自动转发到您的 `app` 的 80 端口       |
| `mcp`| 可选 | 待支持，用于后续提供给语音交互和 AI 使用。      |
| `metadata.yaml` | 必选 | 应用元数据文件，包含应用基本信息及配置       |

> 注意：未来版本可能支持将 `metadata.yaml` 一并打包，目前拆分有利于调试与修改。
## 快速开始



关于开发环境，我们强烈推荐使用 **VS Code Dev Containers**，它能为您提供一个**预配置、干净且隔离**的环境。**然而，我们的应用发布流程与您的开发方式解耦。** 如果您不想使用 Docker 或 VS Code，您仍然可以**用您习惯的任何方式**开发和编译代码，只要最终能按照**约定**将应用打包成可识别的格式，就可以上传至机器人商店使用。

如果您选择不使用 Dev Containers，您可以：
- **本地安装依赖**： 确保您的本地环境（操作系统、编译器、SDK 依赖等）满足对应子项目 README 或 requirements.txt 中列出的所有要求。
- **自由开发**： 使用您偏好的 IDE（如 JetBrains 全家桶、Vim 等）或构建工具进行开发和调试。
- **最终打包**： 无论您采用何种开发方式，您的最终目标是按照宇树的应用发布**约定**编译和打包您的程序。只要最终产物符合上传要求，您就可以将其上传到机器人商店使用。

为方便开发者快速上手开发，以下步骤将以 **VS Code Dev Containers** 作为推荐的开发方式进行说明。

#### 0. 开发容器环境要求

在开始之前，推荐您安装以下软件，当然您也可以使用其他近似产品：

1. **Git:** 用于克隆代码仓库。
2. **Docker:** 用于构建和运行容器环境。
3. **Visual Studio Code (VS Code):** 您的开发编辑器。
4. **VS Code 扩展:**
   - **Dev Containers (开发容器) 扩展**：搜索并安装 `ms-vscode-remote.remote-containers`。

#### 1. 目标架构和跨平台开发
宇树官方开发容器支持 **`amd64`** 和 **`arm64`** 架构，您可在对应架构的机器上直接进行开发和调试。

**关键要求：** 上传至应用商店的二进制程序**必须为目标机器人的系统架构**（通常为 `arm64`）编译。

如果您没有目标架构的物理机，您可参考以下两种跨平台开发方案：

1. **容器跨架构编译 (推荐)：** 利用 Docker 镜像内置的 **QEMU** 支持，您可以在本地 **`amd64`** 电脑上直接运行 **`arm64`** 容器环境。在此环境中，您可以**直接编译**出适用于机器人 `arm64` 系统的二进制文件，实现本地开发、跨架构编译、直接上传的流畅工作流。
2. **传统交叉编译链：** 您也可以选择在本地环境（非容器）中安装并使用**交叉编译工具链**（例如 `aarch64-linux-gnu-*`）来编译目标程序。

选择**方案 1** 可最大限度地利用**宇树提供的预配置环境**，**简化复杂的编译设置**。

目前机器人架构如下：
| 机器人型号 | 系统架构 |
| ---------- | -------- |
| G1         | arm64    |
| GO2         | arm64    |


#### 2. 克隆代码仓库

打开您的终端，将应用模板仓库克隆到本地：

```bash
git clone https://github.com/unitreerobotics/unitree-app-templates.git
cd unitree-app-templates
```

#### 3. 选择并启动开发环境（推荐方式）

每个示例项目都有一个独立的 Dev Container 配置，请根据您想运行的项目，进入对应的子目录，然后启动 VS Code。

##### 示例：启动 Python 控制示例

假设您使用的示例代码位于 `g1_walk_demo` 目录下：

**进入子项目目录：**

```bash
cd g1_walk_demo
code .
```
您也可以直接打开 VS Code，然后使用 `File -> Open Folder` 导航到该目录并打开它。

#### 4. 连接到容器

当 VS Code 启动后，它将检测到子目录中的 **`.devcontainer/devcontainer.json`** 配置文件，并在右下角弹出提示：

> 🚨 **"Folder contains a Dev Container configuration file. Reopen in Container?"**

请点击：**`Reopen in Container`**

VS Code 将自动完成以下步骤：

- 拉取或构建 Docker 镜像（其中已安装好 Python 依赖、宇树 SDK 等）。
- 将您的代码挂载到容器内。
- 重新加载 VS Code 窗口，您现在已连接到一个隔离、配置好的开发环境。

#### 5. 运行您的应用代码

容器连接成功后，您无需在本地安装任何依赖，即可直接在 VS Code 终端中运行您的示例代码。

例如，运行 Python 控制示例：

```bash
python3 ./server/cmd_server.py
```

现在，您就可以开始在完全配置好的环境中调试和开发您的宇树应用了。

#### 6. 调试您的应用

您还可以打开 `mobile_viewer` 目录，运行内置的 HTTP 服务器来调试移动端 UI 界面，详见 [mobile_viewer/README.md](./mobile_viewer/readme.md)。

TODO: 后续也将提供实机调试的说明。

#### 7. 上传您的应用

当您完成代码开发和测试后，您可以使用我们提供的 **CLI 工具** 来打包并上传您的应用，详见 [unitree_cli/README.md](./unitree_cli/readme.md)。

------

**提示：** 如果您想切换到另一个示例（例如 C++ 或 ROS2 示例），只需关闭当前的 VS Code 窗口，回到终端，`cd` 到新的示例目录，再次执行 `code .` 即可。